import "../globals.css";

export const metadata = {
  title: "BrawlGuide - Profile",
  description: "View your Brawl Stars profile stats using your profile ID",
};

export default function ProfileLayout({ children }) {
  return <div className="">{children}</div>;
}
